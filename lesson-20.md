# Working with 3rd party modules using npm and package.json

`npm init`

`npm install`

package.json
Keeps track of dependencies.

package-lock.json
Keeps track of dependencies of dependencies.

node_modules is git ignored.

@types/node

https://www.npmjs.com/package/@types/node

